services:
  postgres:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${DATABASE_NAME:-project_management_db}
      POSTGRES_USER: ${DATABASE_USERNAME:-project_user}
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD:?DATABASE_PASSWORD is required}
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --lc-collate=C --lc-ctype=C"
      USER_DATABASE_NAME: ${USER_DATABASE_NAME:-user_management_db}
      TRAVEL_DB_NAME: ${TRAVEL_DB_NAME:-travel_planner_db}
      FILES_DB_NAME: ${FILES_DB_NAME:-files_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DATABASE_USERNAME:-project_user} -d ${DATABASE_NAME:-project_management_db}"]
      interval: 10s
      timeout: 5s
      retries: 10

  project-api:
    build:
      context: .
      dockerfile: Dockerfile.api
      args:
        APP: project-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      API_PORT: 3002
      API_PREFIX: api
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_NAME: ${DATABASE_NAME:-project_management_db}
      DATABASE_USERNAME: ${DATABASE_USERNAME:-project_user}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD:?DATABASE_PASSWORD is required}
      DATABASE_SSL: ${DATABASE_SSL:-false}
      TYPEORM_SYNCHRONIZE: ${TYPEORM_SYNCHRONIZE:-true}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost,http://localhost:8081,http://localhost:8082}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:3002/api/health', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 10

  user-api:
    build:
      context: .
      dockerfile: Dockerfile.api
      args:
        APP: user-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      USER_API_PORT: 3001
      API_PREFIX: api
      DATABASE_HOST: postgres
      DATABASE_PORT: 5432
      DATABASE_NAME: ${DATABASE_NAME:-project_management_db}
      USER_DATABASE_NAME: ${USER_DATABASE_NAME:-user_management_db}
      DATABASE_USERNAME: ${DATABASE_USERNAME:-project_user}
      DATABASE_PASSWORD: ${DATABASE_PASSWORD:?DATABASE_PASSWORD is required}
      TYPEORM_SYNCHRONIZE: ${TYPEORM_SYNCHRONIZE:-true}
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost,http://localhost:8081,http://localhost:8082}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:3001/api/health', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 10

  files-api:
    build:
      context: .
      dockerfile: Dockerfile.api
      args:
        APP: files-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3004
      API_PREFIX: api
      FILES_DB_HOST: postgres
      FILES_DB_PORT: 5432
      FILES_DB_NAME: ${FILES_DB_NAME:-files_db}
      FILES_DB_USERNAME: ${DATABASE_USERNAME:-project_user}
      FILES_DB_PASSWORD: ${DATABASE_PASSWORD:?DATABASE_PASSWORD is required}
      FILES_STORAGE_DRIVER: ${FILES_STORAGE_DRIVER:-database}
      FILES_MAX_FILE_SIZE_BYTES: ${FILES_MAX_FILE_SIZE_BYTES:-10485760}
      FILES_MIGRATIONS_RUN: "true"
      DATABASE_SSL: ${DATABASE_SSL:-false}
      TYPEORM_SYNCHRONIZE: "false"
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost,http://localhost:8081,http://localhost:8082}
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:3004/api/health', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 10

  bff-api:
    build:
      context: .
      dockerfile: Dockerfile.api
      args:
        APP: bff-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      BFF_PORT: 3000
      PROJECT_API_URL: http://project-api:3002/api
      USER_API_URL: http://user-api:3001/api
      TRAVEL_API_URL: http://travel-planner-api:3003/api
      FILES_API_URL: http://files-api:3004/api
      FILES_MAX_FILE_SIZE_BYTES: ${FILES_MAX_FILE_SIZE_BYTES:-10485760}
      JWT_SECRET: ${JWT_SECRET:?JWT_SECRET is required}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-24h}
      BFF_CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost,http://localhost:8081,http://localhost:8082}
    depends_on:
      project-api:
        condition: service_healthy
      user-api:
        condition: service_healthy
      travel-planner-api:
        condition: service_healthy
      files-api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:3000/health', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 10

  project-web:
    build:
      context: .
      dockerfile: Dockerfile.next
      args:
        APP: project-web
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
      INTERNAL_API_URL: http://bff-api:3000
    depends_on:
      bff-api:
        condition: service_healthy

  jira-web:
    build:
      context: .
      dockerfile: Dockerfile.next
      args:
        APP: jira-web
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
      INTERNAL_API_URL: http://bff-api:3000
    depends_on:
      bff-api:
        condition: service_healthy

  travel-planner-api:
    build:
      context: .
      dockerfile: Dockerfile.api
      args:
        APP: travel-planner-api
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3003
      TRAVEL_DB_HOST: postgres
      TRAVEL_DB_PORT: 5432
      TRAVEL_DB_NAME: ${TRAVEL_DB_NAME:-travel_planner_db}
      TRAVEL_DB_USERNAME: ${DATABASE_USERNAME:-project_user}
      TRAVEL_DB_PASSWORD: ${DATABASE_PASSWORD:?DATABASE_PASSWORD is required}
      USER_API_URL: http://user-api:3001/api
      EXCHANGE_RATES_API_URL: ${EXCHANGE_RATES_API_URL:-https://api.frankfurter.dev/v2}
      EXCHANGE_RATES_CACHE_TTL_SECONDS: ${EXCHANGE_RATES_CACHE_TTL_SECONDS:-900}
      EXCHANGE_RATES_TIMEOUT_MS: ${EXCHANGE_RATES_TIMEOUT_MS:-5000}
      DATABASE_SSL: ${DATABASE_SSL:-false}
      TRAVEL_MIGRATIONS_RUN: "true"
      TYPEORM_SYNCHRONIZE: "false"
      CORS_ORIGIN: ${CORS_ORIGIN:-http://localhost,http://localhost:8081,http://localhost:8082}
    depends_on:
      postgres:
        condition: service_healthy
      user-api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://127.0.0.1:3003/api', r => process.exit(r.statusCode === 200 ? 0 : 1)).on('error', () => process.exit(1))"]
      interval: 10s
      timeout: 5s
      retries: 10

  travel-planner-app:
    build:
      context: .
      dockerfile: Dockerfile.next
      args:
        APP: travel-planner-app
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
        NEXT_PUBLIC_BFF_URL: ${NEXT_PUBLIC_BFF_URL:-}
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-/api}
      NEXT_PUBLIC_BFF_URL: ${NEXT_PUBLIC_BFF_URL:-}
      INTERNAL_API_URL: http://bff-api:3000
    depends_on:
      bff-api:
        condition: service_healthy

  atomdev-landing:
    build:
      context: .
      dockerfile: Dockerfile.next
      args:
        APP: atomdev-landing
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "${HTTP_PORT:-80}:80"
      - "127.0.0.1:${JIRA_HTTP_PORT:-8081}:8081"
      - "127.0.0.1:${TRAVEL_HTTP_PORT:-8082}:8082"
      - "${HTTPS_PORT:-443}:443"
    environment:
      PROJECT_WEB_HOST: ${PROJECT_WEB_HOST:-:80}
      JIRA_WEB_HOST: ${JIRA_WEB_HOST:-:8081}
      TRAVEL_WEB_HOST: ${TRAVEL_WEB_HOST:-:8082}
    volumes:
      - ./docker/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      bff-api:
        condition: service_healthy
      project-web:
        condition: service_started
      jira-web:
        condition: service_started
      travel-planner-app:
        condition: service_started
      atomdev-landing:
        condition: service_started

volumes:
  postgres_data:
  caddy_data:
  caddy_config:
